apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cluverse-tls
  namespace: cluverse
spec:
  secretName: cluveres-tls-secret
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: cluverse.tn
  dnsNames:
  - cluverse.tn
  - www.cluverse.tn
